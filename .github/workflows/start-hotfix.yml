name: (▶) Start Hotfix

on: workflow_dispatch

jobs:
  start-hotfix:
    uses: sitepark/github-project-workflow/.github/workflows/maven-start-hotfix.yml@main
    with:
      ref: ${{ github.ref }}

